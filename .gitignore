# Built binaries
/xxdrive-server
/xxdrive-cli

# Server runtime data
/data/

# Android build artifacts
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.apk

# OS / editor noise
.DS_Store
*.swp

# Skippy runtime bookkeeping — never tracked
.skippy/

# Local go build cache (used when the sandbox's default GOCACHE is read-only)
.gocache/
