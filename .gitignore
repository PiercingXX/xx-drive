# Built binaries
/xxdrive-server
/xxdrive-cli

# Server runtime data
/data/

# Android build artifacts
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.apk

# OS / editor noise
.DS_Store
*.swp

# Skippy runtime bookkeeping — never tracked
.skippy/

# Local toolchain shim dir (workspace_child_env prepends .venv/bin to the gate
# PATH; the go shim there delegates to bin/go). Never tracked.
.venv/

# Local go build cache (used when the sandbox's default GOCACHE is read-only)
.gocache/

# Estate mill/audit scratch — never tracked
.nagatha/
.bilby/
